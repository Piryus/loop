# Backend (server/linear.ts) — a dedicated Linear workspace for feedback.
# Only the API key is required; the team is auto-discovered when the workspace
# has a single team. Set LINEAR_TEAM_ID only to target a specific team.
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxx
# LINEAR_TEAM_ID=00000000-0000-0000-0000-000000000000
